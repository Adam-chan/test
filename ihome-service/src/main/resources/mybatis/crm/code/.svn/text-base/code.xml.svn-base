<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="code">
	
	<!-- 获取编号 -->
	<select id="getCode" parameterType="java.lang.String" resultType="codeModel">
		SELECT
			c.CODE_NUMBER as codeNumber,
			c.ID as id
		FROM
			BASE_CODE c
		WHERE
			c.CODE_TYPE = #{_parameter}
	</select>
	
	<!-- 根据id和类型更新编号-->
	<update id="updateCode" parameterType="java.util.Map">
       update BASE_CODE c set c.CODE_NUMBER =  #{code} where  c.ID =  #{id} and c.CODE_TYPE = #{codeType} 
	</update> 
	
	<!-- 插入编号信息 -->
	<insert id="insertCode" parameterType="java.util.Map">
		<selectKey resultType="java.lang.Integer" order="BEFORE" keyProperty="id">
			SELECT BASE_CODE_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO BASE_CODE (ID,CODE_TYPE,CODE_NUMBER) VALUES(#{id},#{codeType},#{code})
	</insert>
	
	
</mapper>